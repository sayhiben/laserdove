# Runtime compatibility for Python < 3.11
tomli>=2.0.1; python_version < '3.11'

# Developer/tests (install with `pip install .[test]` when using pyproject)
# pytest>=7.4
# pytest-cov>=4.1

# Hardware transport (UDP mode default; USB removed)
